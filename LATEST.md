<!-- このファイルは scripts/finalize.sh が自動生成します。直接編集しないでください。 -->
<!-- 編集する場合は digests/2026-08-27.md を直してから finalize.sh を再実行してください。 -->

# Claude 日次ダイジェスト — 2026-08-27 (木)

Claude Code が **2.1.247** に上がりました。説明つき5件、一行で流すもの28件。
API 側では管理用の機能が各言語から使えるようになっています。

---

## ⚠️ 今の使い方が変わるもの

**新しい版はまだ手元に入りません（数時間待ち）**
CHANGELOG は 2.1.247 になっていますが、実際にインストールできる npm はまだ 2.1.246 です。
`claude update` が空振りしても正常です。数時間待てば入ります。手動で入れようとしないでください。

---

## 今日の変更（説明つき）

### 長い作業が途中で止まりにくくなりました

Claude は大きな仕事をするとき、小さな作業係を何人か呼び出して手分けさせます。
これまでは、その作業係が使うモデルに一時的につながらないと、そこで全部止まっていました。
今回から、別のモデルに切り替えて自動で続けるようになります。

*気になるなら聞けること: 「今、途中で止まりがちな作業ってある?」*

### エラーが大量に出てもセッションが固まらなくなりました

自動実行の途中で何かが数MB分のエラーを吐くと、それが会話に流れ込んで
「話が長すぎます」という状態になり、セッションごと動かなくなることがありました。
今回からエラー出力が溢れても、セッションは生き残ります。

*気になるなら聞けること: 「うちの自動実行で、これに当たってたやつある?」*

### 長い会話で「昔の話を要約して圧縮する」タイミングが後ろにずれました

Claude は会話が長くなると、古いやり取りを自動で要約して圧縮します（auto-compact）。
その圧縮が始まる位置が後ろに移り、圧縮される前により多くを覚えていられるようになりました。
※ 対象は Sonnet 5 です。Opus 5 を使っている場合は今回は変わりません。

*気になるなら聞けること: 「長い分析をやらせるとき、どのモデルが向いてる?」*

### Claude が自分で不具合の報告文を書けるようになりました

セッション中に何かおかしくなったとき、Claude がその状況をまとめた報告文を下書きし、
`/feedback` で中身を確認してから送れるようになりました（`feedbackDrafts` 設定でオフにできます）。

### API の料金を下げるための診断ツールが増えました

`/claude-api cost-optimize` というコマンドが追加されました。
既存のプロジェクトが API に払っている額を分析し、下げられる要因（同じ内容の使い回し、
無駄なやり取りの量、まとめ処理、モデルの選び直し）を1つずつ測りながら潰していきます。

*気になるなら聞けること: 「今のRoutine 8本、月いくらかかってる?」*

---

## 一行で流すもの（28件）

**直った不具合**
- Zed を使っている場合に `/terminal-setup` がキーボード設定を丸ごと上書きしていた
- 設定ファイルをシンボリックリンクで管理していると、そのリンクが消されることがあった
- ロシア語などラテン文字以外のキーボード配列で Ctrl のショートカットが効かなかった
- マウス操作の信号が文字として入力欄に紛れ込むことがあった
- `/rename` で名前を変えたのに、実際には変わっていないのに成功と表示された
- `/compact`（会話の圧縮）が、指定した設定ではなく既定の設定で要約していた
- 停止したセッションが「起動中…」のまま永久に表示され続けた
- 出力の書き出しに失敗したとき、メモリが際限なく増えていった
- `/install-github-app` を SSH 越しに使うとき、コピーできていないのに成功と表示された
- クラウドセッションが、途中でコンテナが再起動すると無言で止まっていた
- クラウドセッションで権限モードを切り替えた直後、前の設定が表示されることがあった
- 実行中のプラグインが、別の場所への再インストールで壊されることがあった
- Remote Control で始めたセッションが作業中の変更を報告していなかった
- 自前サーバーで動かすセッションが、起動前なのに「実行中」と報告していた

**変わった挙動**
- セッション間のメッセージが既定で1行に畳まれるように（全文は Ctrl+O で展開）
- Bash の権限確認画面から auto モードへ1キーで移れるように
- プラグイン配布元の名前に不可視文字が混ざっているものを拒否するように
- 表示されるリンクのうち、危険な形式のものはただの文字として表示するように
- 組織の管理設定が読めないとき、起動時点で終了するように

（ほか、社内向け・開発者向けの細かい改善が9件）

**API 側**
- 組織の管理機能（メンバー・招待・APIキー・利用状況など）が Python / TypeScript / Go / Java /
  PHP / Ruby / C# の各言語から使えるようになりました
- Cowork と Claude Code のセッション記録を取り出す機能が正式版になりました（法人管理者向け）

---

## 📊 定点観測

| 項目 | 現在値 |
|---|---|
| Claude Code (CHANGELOG) | **2.1.247** |
| Claude Code (npm 公開) | 2.1.246 *(反映待ち)* |
| Claude Fable 5 | `claude-fable-5` $10 / $50 per MTok |
| Claude Opus 5 | `claude-opus-5` $5 / $25 per MTok |
| Claude Sonnet 5 | `claude-sonnet-5` $2 / $10 per MTok |
| Claude Haiku 4.5 | `claude-haiku-4-5` $1 / $5 per MTok |
| ソース取得 | 8件中 8件 成功 |

**Cowork について（二次情報 / 未確認）**
Cowork の公式チェンジログはこの収集環境から直接取得できません（egress ブロック）。
検索では「メモリ機能がチャットと Cowork の両方に対応」「Claude Desktop v1.37937.1 が
CLI 2.1.246 を同梱」といった情報が見えていますが、一次情報で裏が取れていません。

---

## 🔗 出典

- [Claude Code CHANGELOG](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)
- [Claude Platform リリースノート](https://platform.claude.com/docs/en/release-notes/overview)
- [モデル一覧](https://platform.claude.com/docs/en/models/overview)
- [料金](https://platform.claude.com/docs/en/about-claude/pricing)

---

気になるものがあれば、そのまま聞いてください。

---

過去のダイジェスト一覧は [INDEX.md](INDEX.md) を参照してください。
