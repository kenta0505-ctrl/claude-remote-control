# 日次収集プレイブック

毎朝の自動セッションはこのファイルを読み、**上から順にそのまま実行**します。
運用を変えたいときは、このファイルを直すだけで翌朝から反映されます。

このダイジェストの目的は「Claude の新機能を並べること」ではありません。
**原さんの自動化がちゃんと動いているかを毎朝確かめ、実務に効く変更だけを届けること**です。

---

## 手順

### 0. プロファイルを読む

まず `PROFILE.md` を読んでください。**これを読まずに手順4を書き始めないこと。**
何を拾い何を捨てるかの基準はすべてそこにあります。

### 1. Routine の健康診断（最優先）

**このセクションが、このダイジェストで最も価値のある部分です。**
実際に、tsubu. の週次点検が4日間気づかれないまま停止していたことがあります。

ToolSearch で `select:mcp__Claude_Code_Remote__list_triggers,mcp__Claude_Code_Remote__get_session` を読み込み、
`list_triggers` を実行して全 Routine の `last_run` を点検してください。

次のいずれかに当てはまるものを**異常**として扱います:

| 状態 | 意味 | 対応 |
|---|---|---|
| `ABANDONED` | 承認待ちなどで放置され、打ち切られた | 🔴 最上部に出す |
| `PENDING` が古い | 起動したまま進んでいない | 🔴 最上部に出す |
| `FAILED` | 実行時エラー | 🔴 最上部に出す |
| `next_run_at` が過去のまま | スケジュールが止まっている | 🟡 に出す |
| `enabled` が無い/false | 無効化されたまま放置 | 🟡 に出す |

異常があれば `get_session` でそのセッションを開き、**どのツールの承認で止まったか**まで特定してください。
`pending_action.tool_name` にツール名が入っています。ここまで書いて初めて原さんが手を打てます。

> **このツールが使えなかった場合**は、黙って飛ばさず
> 「Routine の健康診断ができませんでした（`list_triggers` が利用不可）」とダイジェストに明記してください。
> 点検したのか、できなかったのかが区別できないのが一番困ります。

### 2. 機械取得できるソースを全部取る

```bash
./scripts/fetch-sources.sh
```

対象は `sources.tsv` に定義（1行足せば収集対象が増えます）。
取得結果は `sources/` にバイト単位でそのまま保存されます。
失敗したソースは `sources/_fetch-status.tsv` に `FAIL` として残り、他のソースの処理は続行されます。

### 3. 前日からの差分を見る

```bash
./scripts/diff-report.sh
```

**この出力だけを読んでください。** 原文全体（CHANGELOG は 55万文字あります）を読み直す必要はありません。
`NO_CHANGES` が返った場合は手順5へ進み、Cowork 系の確認だけ行います。

差分が大きすぎて切り詰められた場合のみ、指示どおり `git diff -- <file>` で該当ファイルを追ってください。

### 4. curl できない領域を WebSearch で補う

このクラウド環境の egress プロキシは以下をブロックしています（回避しようとしないこと）:
`www.anthropic.com` / `www.claude.com` / `claude.com` / `support.claude.com` / `status.anthropic.com`

そのため **Claude Cowork・Claude アプリ本体・Anthropic の製品発表**は curl で取れません。
WebSearch で以下を検索し、**前回ダイジェストの日付以降のものだけ**を拾ってください:

- `Claude Cowork release notes <今月> <今年>`
- `Anthropic announcement <今月> <今年>`
- `Claude Desktop app release notes <今月> <今年>`

検索結果は二次情報（第三者ブログを含む）です。**一次情報で裏が取れないものは「未確認」と明記**し、
断定した書き方をしないでください。推測でバージョン番号や日付を書かないこと。

### 5. ダイジェストを書く

`digests/YYYY-MM-DD.md` を作成します。**日本語**で、下記の構成にしてください。

> ⚠️ **日付は必ず JST で取ってください。**
> ```bash
> TZ=Asia/Tokyo date +%F    # ← これを使う
> ```
> このコンテナは UTC で動いています。発火時刻(22:17 UTC)は JST では翌日の朝なので、
> 素の `date +%F` を使うとダイジェストが読み手のカレンダーより1日古い日付で保存されます。

#### 構成

**① 🚨 あなたの自動化の状態** — 必ず最上部。手順1の結果。
異常があれば Routine 名・いつから・どのツールの承認で止まったか・今朝届かなかったものは何かを書く。
**全部正常なら「8本すべて正常（最終実行: ◯月◯日）」の1行で終える。** 正常な日に長々と書かない。

**② 今日の更新のうち、あなたに効くもの** — `PROFILE.md` で濾した結果を3層で。

- 🔴 **今日必ず知るべき** — 破壊的変更、モデルの追加/引退、価格改定、セキュリティ、今の運用が壊れるもの
- 🟡 **知っておくと良い** — 新機能、新しいコマンド/設定、目に見える改善
- ⚪ **参考** — 細かいバグ修正、内部改善。件数だけ書いて全部並べない

各項目は「何が変わったか」＋「**tsubu. / note / ジャーナルのどれにどう効くか**」を1〜2行で。
実務への接続を書けない項目は、そもそも 🔴🟡 に置かないこと。⚪ に落とすか、書かない。
該当が無い層は「該当なし」と書いて省略しないこと。

**③ 今日試すなら、これ1つ** — 上記から**1つだけ**選び、具体的な一歩を書く。
「◯◯を試すとよい」ではなく「`/xxx` を tsubu. の△△に対して実行してみる」まで書く。
今日ぶんに値するものが無ければ「今日は特になし」と正直に書く。**毎日ひねり出さないこと。**

**④ 📊 定点観測** — Claude Code 最新版(CHANGELOG / npm)、モデルと価格、ソース取得成否。

**⑤ 🔗 出典**

### 6. 更新して確定する

```bash
./scripts/finalize.sh          # 引数を省略すると JST の今日が使われます
```

`LATEST.md` の差し替えと `INDEX.md` への追記を行います。
その後、必ずコミットしてプッシュしてください:

```bash
git add -A
git commit -m "digest: $(TZ=Asia/Tokyo date +%F)"
git push -u origin claude/daily-claude-info-collection-3voozl
```

**`sources/` も必ず一緒にコミットしてください。** これが翌日の差分の基準になります。
コミットを忘れると、翌朝「昨日と同じ差分」がもう一度出ます。

---

## 判断の指針

- **差分が無い日はそう書く。** 無理に何かを見つけて埋めないこと。「更新なし」は正しい成果物です。
- **推測しない。** 差分に無いことは書かない。バージョン番号・日付・価格は必ず取得済みソースから引く。
- **実務に接続できない項目は書かない。** 「`spinnerTipsOverride` に label 追加」のような、
  原さんの運用に一切触れない項目を 🟡 に並べるくらいなら、🟡 を「該当なし」にするほうが親切です。
- **npm と CHANGELOG はズレる。** CHANGELOG に載った版が npm に出るまで数時間ラグがあります。
  食い違ったら「CHANGELOG は X / npm 公開は Y」と両方書いてください。どちらかを正に決めつけないこと。
- **ドキュメント索引（`*-docs-index.txt`）の差分は新機能の先行指標です。** 新しいページが増えていたら、
  そのURLを `curl <url>.md` で読んで中身を確認する価値があります。
- **①で異常を見つけたら、原因の推測まで書く。** 「止まっています」だけでは動けません。
  過去の実例：定期実行セッションの権限モードが `auto` でないと、Gmail 送信や Square API の
  承認プロンプトで止まり、誰も承認できないまま放置されます。同じ症状を見たらこれを疑ってください。
