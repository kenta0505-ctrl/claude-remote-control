# 日次収集プレイブック

毎朝の自動セッションはこのファイルを読み、**上から順にそのまま実行**します。
運用を変えたいときは、このファイルを直すだけで翌朝から反映されます。

---

## 手順

### 1. 機械取得できるソースを全部取る

```bash
./scripts/fetch-sources.sh
```

対象は `sources.tsv` に定義（1行足せば収集対象が増えます）。
取得結果は `sources/` にバイト単位でそのまま保存されます。
失敗したソースは `sources/_fetch-status.tsv` に `FAIL` として残り、他のソースの処理は続行されます。

### 2. 前日からの差分を見る

```bash
./scripts/diff-report.sh
```

**この出力だけを読んでください。** 原文全体（CHANGELOG は 55万文字あります）を読み直す必要はありません。
`NO_CHANGES` が返った場合は手順3へ進み、Cowork 系の確認だけ行います。

差分が大きすぎて切り詰められた場合のみ、指示どおり `git diff -- <file>` で該当ファイルを追ってください。

### 3. curl できない領域を WebSearch で補う

このクラウド環境の egress プロキシは以下をブロックしています（回避しようとしないこと）:
`www.anthropic.com` / `www.claude.com` / `claude.com` / `support.claude.com` / `status.anthropic.com`

そのため **Claude Cowork・Claude アプリ本体・Anthropic の製品発表**は curl で取れません。
WebSearch で以下を検索し、**前回ダイジェストの日付以降のものだけ**を拾ってください:

- `Claude Cowork release notes <今月> <今年>`
- `Anthropic announcement <今月> <今年>`
- `Claude Desktop app release notes <今月> <今年>`

検索結果は二次情報（第三者ブログを含む）です。**一次情報で裏が取れないものは「未確認」と明記**し、
断定した書き方をしないでください。推測でバージョン番号や日付を書かないこと。

### 4. ダイジェストを書く

`digests/YYYY-MM-DD.md` を作成します。**日本語**で、以下の3層構成にしてください。

- 🔴 **今日必ず知るべき** — 破壊的変更、モデルの追加/引退、価格改定、セキュリティ、今の使い方が壊れるもの
- 🟡 **知っておくと良い** — 新機能、新しいコマンド/設定、目に見える改善
- ⚪ **参考** — 細かいバグ修正、内部改善。件数だけ書いて全部並べない

各項目は「何が変わったか」＋「自分にとって何が嬉しい/困るか」を1〜2行で。
原文の直訳ではなく、**使う側の言葉**に翻訳してください。

該当が無い層は「該当なし」と書いて省略しないこと（層が消えると、
確認したのか漏れたのか後から区別できなくなります）。

### 5. 更新して確定する

```bash
./scripts/finalize.sh YYYY-MM-DD
```

`LATEST.md` の差し替えと `INDEX.md` への追記を行います。
その後、必ずコミットしてプッシュしてください:

```bash
git add -A
git commit -m "digest: YYYY-MM-DD"
git push -u origin claude/daily-claude-info-collection-3voozl
```

**`sources/` も必ず一緒にコミットしてください。** これが翌日の差分の基準になります。
コミットを忘れると、翌朝「昨日と同じ差分」がもう一度出ます。

---

## 判断の指針

- **差分が無い日はそう書く。** 無理に何かを見つけて埋めないこと。「更新なし」は正しい成果物です。
- **推測しない。** 差分に無いことは書かない。バージョン番号・日付・価格は必ず取得済みソースから引く。
- **npm と CHANGELOG はズレる。** CHANGELOG に載った版が npm に出るまで数時間ラグがあります。
  食い違ったら「CHANGELOG は X / npm 公開は Y」と両方書いてください。どちらかを正に決めつけないこと。
- **ドキュメント索引（`*-docs-index.txt`）の差分は新機能の先行指標です。** 新しいページが増えていたら、
  そのURLを `curl <url>.md` で読んで中身を確認する価値があります。
